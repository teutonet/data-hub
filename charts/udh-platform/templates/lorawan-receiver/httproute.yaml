{{- if .Values.sensorIngestion.lorawanReceiver.ingress.enabled -}}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "data-hub.name" (dict "context" . "name" "lorawan-receiver") }}
  labels: {{- include "common.labels.standard" . | nindent 4 }}
    app.kubernetes.io/component: {{ "lorawan-receiver" }}
spec:
  hostnames:
  - {{ include "sensor-ingestion.api.hostname" . }}
  parentRefs:
  - name: {{ include "data-hub.name" (dict "context" . "name" "api") }}
    sectionName: https
  rules:
  - backendRefs:
    - name: {{ include "data-hub.name" (dict "context" . "name" "lorawan-receiver") }}
      port: 80
    matches:
    - path:
        type: PathPrefix
        value: /api/v1/sensordata
{{- end -}}
